package `fun`.fizzy.`fun`.fizzy.lc.easy

/**
 * @description documentation of the issue
 * @problem brief of the task
 * @parameters details and types
 * @definitions terms and concepts
 * @constraints limitations or restrictions
 * @assumptions assumptions, including edge cases
 * @complexity time and space
 * @improvements optimization suggestions
 * @tags relevant categories
 */
class Data {

    /**
     * @problem in increasing order, prints the max value for each integer data type
     * @analysis O(1) print
     */
    fun printMaxIntegerTypes() {
        println(Byte.MAX_VALUE)
        println(Short.MAX_VALUE)
        println(Integer.MAX_VALUE)
        println(Long.MAX_VALUE)
    }
}