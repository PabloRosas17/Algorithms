package `fun`.fizzy.tools.datastructures.dat

/**
 * @desc defines a slot of direct memory
 * @param key as the location for the data
 * @param element as the data stored
 */
data class Table(var key: Int, var element: Int)